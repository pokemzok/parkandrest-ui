import { HasAuthDirective } from './has-auth.directive';

describe('HasAuthDirective', () => {
  it('should create an instance', () => {
    const directive = new HasAuthDirective();
    expect(directive).toBeTruthy();
  });
});
